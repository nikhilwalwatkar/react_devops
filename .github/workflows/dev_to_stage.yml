name: Auto-Merge Dev to Test

on:
  push:
    branches:
      - test

jobs:
  auto-merge:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Merge code to test branch
        run: |
          git checkout dev
          git merge test
          git push origin dev
